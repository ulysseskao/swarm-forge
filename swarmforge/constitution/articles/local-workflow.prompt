# Local Workflow Rules

## QA Handoffs
- QA handoff 不會中斷目前工作。如果正在工作時收到 QA wake-up，忽略它，並讓 `done_with_current.sh` 在目前工作完成後送達下一個 queued task 或 batch。
- 當 helper-delivered task 來自 QA 時，`ready_for_next.sh` 已經合併該 commit。除了 specifier 之外，每個 agent 都必須執行 unit tests 與 acceptance tests，並修正任何 failures。然後執行 `done_with_current.sh`。不要向 downstream 送出 `git_handoff`。不要發明 `git merge`。除了 verification 和 role prompt 中任何 QA-completion steps 之外，不要對該 QA handoff 套用其他 role-specific work。
