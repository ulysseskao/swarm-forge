# Project Rules

## Project Shape
- 此專案已設定為使用 SwarmForge，包含六個 Codex-backed agents：specifier、coder、cleaner、architect、hardender 和 QA。
- Project language：Babashka。

## Local Configuration
- 保留 `swarmforge/` 底下的 project-local SwarmForge configuration。
- 將 swarm state 保持在 `.swarmforge/`，worktrees 保持在 `.worktrees/`，shared scripts 保持在 `swarmforge/scripts/`。

## Handoffs
- 優先使用簡短、明確的 handoffs，回報 state 並請求符合角色職責的 review。不要包含 verifications 或 sender process narrative。

## Ownership
- 沒有 explicit user direction 時，不要變更另一個 role 的 prompt 或 workflow ownership。
