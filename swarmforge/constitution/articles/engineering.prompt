# Engineering Rules

## Startup Tools
- 啟動時，直接從列出的 `github.com/unclebob/...` repositories 取得專案語言所需的每個 CRAP、mutation 與 DRY tool 的最新版，並讓每個工具都可執行。
- 在 install 或 build 前，先將每個列出的 repository resolve 到最新可用的 upstream version。
- 當目前環境可以 fresh GitHub install/build 時，不要依賴過期的 cached、vendored 或 preinstalled copies。
- Language tool table：
  - Go：使用 `go install` 安裝；mutation `github.com/unclebob/mutate4go`、CRAP `github.com/unclebob/crap4go`、DRY `github.com/unclebob/dry4go`。
  - Clojure：使用 Clojure CLI/deps.edn 安裝；mutation `github.com/unclebob/clj-mutate`、CRAP `github.com/unclebob/crap4clj`、DRY `github.com/unclebob/dry4clj`。
  - Java：使用 Maven（`mvn`）安裝；mutation `github.com/unclebob/mutate4java`、CRAP `github.com/unclebob/crap4java`、DRY `github.com/unclebob/dry4java`。

## Language Defaults
- 對 Clojure projects，在可行時優先使用 Babashka。
- 對 Clojure 或 Babashka projects，撰寫 Speclj specs，而不是 `clojure.test`。
- 使用 `swarm_tool.sh require speclj` 安裝 Speclj。如果缺少，執行 `swarm_tool.sh ensure speclj`。
- 如果 Speclj spec file 有變更，在執行 tests 前，先執行 `swarm_tool.sh require speclj-structure-check`（缺少時 ensure）並檢查該 spec file。
- 對 Java projects，避免使用 Maven 執行 tests；改為建立 dedicated test runners 並執行它們。

## Design And Testability
- 以小而可 review 的 increments 工作。
- 優先採用能支援目前行為、並為下一步留下清楚選項的最簡單設計。
- 讓 tests 貼近正在變更的行為。
- 將 testable modules 與 environmentally unsuitable modules 分離；後者包含會開 GUI、依賴外部裝置、拋出環境錯誤、發出 system errors，或在 automated tests 下 hang 住的 modules。最大化 testable code，最小化 unsuitable boundary。
- 靠近 IO 的 modules 不可重新實作 domain 問題。如果某個 high-level module 已經回答了它，就呼叫該 module 並轉換其結果。即使 dependency arrow 已經指向內部，重走一遍同樣的事實仍然是缺陷。
- 只有 testable modules 應參與會執行 tests 的工具，包括 unit tests、acceptance tests、coverage、mutation testing、CRAP analysis、會呼叫 tests 的 DRY analysis，以及 property tests。
- 將 property tests 與 normal verification 分開。除非該角色負責 property-test verification，或 user 明確要求 property tests，否則不要在 normal unit coverage、Gherkin acceptance mutation、language mutation tools、CRAP 或 coverage commands 中包含 property-test tags。

## Acceptance Pipeline
- 使用 github.com/unclebob/Acceptance-Pipeline-Specification 進行 Gherkin acceptance tests。
- Acceptance Pipeline Specification 提供 `gherkin-parser`、`ir-dry-checker` 與 `gherkin-mutator`。請用 project-local helper 安裝它們；不要搜尋 `$HOME` 或執行 `find` 尋找 binaries。
- 在 Tool Startup 時，對每個需要的 APS tool 執行 `swarm_tool.sh require <tool>`。如果缺少，精確執行 `swarm_tool.sh ensure <tool>`。
- Two-arg forms：
  - `gherkin-parser <feature> ./tmp/<stem>.json`
  - `ir-dry-checker <ir> ./tmp/<stem>.dry.json`
- 對 `gherkin-parser`、`ir-dry-checker`、`gherkin-mutator` 及相關 APS support commands，優先使用 Babashka APS tools。
- 只有在 Babashka APS tools 無法在目前 project environment 運作時，才使用 Go-based APS tools。
- Project-specific acceptance pipeline components 包含 acceptance entrypoint generator、acceptance runtime、project step handlers、runner adapter 與 convenience scripts。
- Gherkin acceptance mutation 指執行 `gherkin-mutator` 來 mutate Gherkin example values。
- Gherkin acceptance mutation 執行期間必須定期回報 progress/status，讓 agents 能區分正常的 long-running work 與 hang。

## Verification
- 執行 language、build 或 test commands 前，優先使用 assigned worktree 內的 project-local cache/configuration paths。避免使用會寫到專案外、可能觸發 sandbox 或 permission restrictions 的預設 cache locations。
- Constitution tools 一次只執行一個。不要並行執行 CRAP、DRY、coverage、language mutation、Gherkin mutation 或 structure-check。
- 接受 worker limit 的 tools 必須使用 `--max-workers 4` 或 `--workers 4`。
- Function mutation 是相對於 source manifest 的 differential。不要傳入 `--mutate-all`。
- 用 language mutation tool 的 scan/count mode 掃描 changed 與 new source files，作為「該 module 可能混合了多種職責」的提示。當一個 source file 做了不只一件事時才拆分。不要為了追求 mutation-site 數量而拆分單一職責的 module。拆分時保留 mutation manifests；不要手動編輯它們。
- Gherkin mutation 是相對於 feature manifest 的 differential（`--level hard`）。不要傳入 `--level full`。
- 依序執行 acceptance generation 與 acceptance tests。
- 避免讓 whole-suite language test commands 與 acceptance generation 並行。
- 只要 project 有 relevant local verification command，就在 handoff 前執行。

## Guardrails
- 不要發明 project-local CRAP、DRY、mutation 或 coverage proxies。安裝並執行 constitution tools（`crap4clj` 搭配 cloverage、`dry4clj`、`clj-mutate`、speclj，或 language table 中的工具）。不要把自製的 `bb crap` / `bb coverage` / `bb mutation-count` task 當成那些工具。
- 不要手動編輯 mutation testing 或 Gherkin acceptance mutation manifests；讓 approved mutation tools 在正常執行中更新這些 manifests。
- 除非 task 需要，否則不要 commit unrelated local changes 或 generated artifacts。
- 在依賴不熟悉的 command 前，先查看 local help 或 project documentation。
