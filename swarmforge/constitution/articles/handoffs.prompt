# Handoff Rules

## Sending Handoffs
- 在指定的 worktree 中，於 `./tmp/` 下撰寫只包含 structured headers 的 draft handoff file，然後執行 `swarm_handoff.sh <draft-file>`。
- `swarm_handoff.sh` 會拒絕位於 `/tmp` 和 `.swarmforge/handoffs/outbox/tmp/` 的 drafts。
- `swarm_handoff.sh --help` 會印出 usage。不要把 `--help` 當成 draft path 傳入。
- 只使用以下 message types：
  - `git_handoff`
  - `note`
- 除非 user、role prompt 或 constitution 明確指示，否則不要送出 `note` handoffs。
- 如果因為模糊、矛盾或 test/specification 衝突而被阻塞，請用 `pack_dashboard_request.sh clarify ./tmp/question.txt` 詢問 operator。不要在 pane 裡提問。除非上述明確權威來源之一要求送出 `note`，否則不要送 `note` handoff。
- 對於 `git_handoff`，先 commit，再撰寫 draft。Helper 會從 sender worktree HEAD 填入 `commit` 與 `artifacts`；不要手打 SHA。如果 `SWARMFORGE_ROLE` 未設定，helper 會從 `roles.tsv` 與這個 worktree 推斷角色。
- 如果你是在包含 specifier 的 pack 裡擔任 master agent，當工作就緒時 queue `git_handoff`。不要在 pane 裡要求 approval；operator 會使用 Attention。
- 不要加入額外 git headers（`coverage`、CRAP、evidence）。如果需要 evidence，helper 會寫入。
- 對於 `git_handoff`，請寫：

```text
type: git_handoff
to: <role>[,<role>...]
priority: NN
task: <short-stable-task-name>
```

- 完成 forward inbound task 後，無論變更內容為何，都一定要送出 `git_handoff` 給 chain 中的下一個角色。即使只是 formatting-only、manifest-only、audit-only、generated metadata 或其他 non-functional churn，也仍然需要沿 chain forward。
- reverse（`non-forwarding`）inbound `git_handoff` 只做 merge。Merge 後執行 `done_with_current`。不要為該 inbound 送出 `git_handoff`。當 inbound 是 `non-forwarding` 時，helper 會拒絕 `git_handoff`。
- 為同一個 task forward work 時，保留收到的 task name。
- master agent 使用既有 New Task / board card name 作為 `task:`。不要發明名稱。
- 對於 `note`，請寫：

```text
type: note
to: <role>[,<role>...]
priority: NN
message: <one line, max 80 chars>
```

- 如果 `swarm_handoff.sh` 回報 validation errors，修正 draft 後重新執行。
- 成功送出後，helper 會移除 draft file。如果需要手動移除 stale draft，請使用 `rm <draft-file>`，不要使用 `rm -f`。
- 不要撰寫冗長的 handoff bodies。Helper 會產生實際送出的 payload。
- 不要直接送 tmux notifications。
- 不要手動編輯、merge、stage 或 commit handoff runtime state。

## Receiving Handoffs
- 收到通知時，執行 `ready_for_next.sh`。如果 `SWARMFORGE_ROLE` 未設定，helper 會從 `roles.tsv` 與這個 worktree 推斷角色。
- `ready_for_next.sh` 會 dispatch 到針對你角色設定的 task 或 batch helper。對於 `git_handoff`，它會合併 inbound commit（`merge_and_process.sh <sender> <commit>`）。
- 隨著工作在 pack 中往下游移動，結構會逐步改善。在 reverse（`non-forwarding`）`git_handoff` 中，inbound tree 就是結構。把此角色目前的 task replay 到該形狀上。不要為了保留本地工作而維持 pre-inbound layout。在 forward `git_handoff` 中，此角色目前的 tree 就是結構。把 inbound work replay 到該形狀上。不要為了省 merge work 而採用 inbound layout。
- 如果 merge_and_process.sh 或 ready_for_next 回報 merge conflict，請解決每一個 conflicted file、對它們執行 `git add`，然後 commit。Conflict resolution 是你的職責。不要發明新的 `git merge`。同一棵 tree 上的 parallel cards 發生 conflict 是預期情況。
- 如果它印出 `NO_TASK`，停止等待工作。
- 如果它印出 `TASK: <path>`，把印出的 `PAYLOAD` 視為 task。
- 如果它印出 `TASK_NAME: <name>`，將其作為你從該 task forward 任何工作時的 stable task name。
- 如果它印出 `BATCH: <path>`，把每個印出的 `BATCH_ITEM` 依 helper 送達順序視為目前 batch 的一部分。
- 只使用 helper scripts 印出的 task information。
- 如果正在處理 task 時收到 tmux wake-up，忽略它。
- 當 task 或 batch 完全完成時，執行 `done_with_current.sh`。在 reverse（`non-forwarding`）merge 之後，這就是 completion；不要為該 inbound queue `git_handoff`。
- `note` handoffs 也是 tasks；閱讀或處理 note 後，先執行 `done_with_current.sh`，再接受任何其他 handoff。
- 如果 `done_with_current.sh` 印出 `MAIL_WAITING`，把 `ready_for_next.sh` 當成新 turn 執行。不要把 done output 當成下一個 merge。
- 如果 done helper 印出 `NO_TASK`，停止等待工作。
- restart 時，執行 `ready_for_next.sh` 並遵循其輸出。
