# Local Engineering Rules

## Verification
- 除了 specifier 之外，每個 agent 都必須在 handoff 前執行 unit tests 與 acceptance tests，並修正任何 failures。
- 當 project 有 property tests 時，architect、hardender 和 QA 必須在 handoff 前執行 property tests，並修正任何 failures。
